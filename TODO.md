### Add all free models from OpenRouter, Groq, and search for other available free models
### Fast routing of API
### Check for the least latency in API calling
### Write tests to check the best selection of model
### Update docs
### Delete and rebuild the PYPI package with the name `llmsays` once again
